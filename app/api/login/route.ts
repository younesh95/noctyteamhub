import {SUPABASE_URL,SUPABASE_KEY} from '../../../lib/supabase';
export async function POST(request:Request){try{const {username,password}=await request.json() as {username:string,password:string};if(typeof username!=='string'||typeof password!=='string'||username.length>24||password.length>256)return Response.json({error:'Identifiants invalides.'},{status:400});const resolve=await fetch(SUPABASE_URL+'/functions/v1/username-login',{method:'POST',headers:{apikey:SUPABASE_KEY,'Content-Type':'application/json'},body:JSON.stringify({username,password})});if(resolve.status===404)return Response.json({error:'Le service de connexion par pseudo doit être installé sur Supabase. Consultez le guide de configuration livré.'},{status:503});const data=await resolve.json();return Response.json(data,{status:resolve.status,headers:{'Cache-Control':'no-store'}})}catch{return Response.json({error:'Service de connexion indisponible.'},{status:503})}}

